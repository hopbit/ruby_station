# RubyStation

Learn Ruby by making the tests green.

1. Reset to ```start``` tag:

  ```
  git reset --hard start
  ```

2. Install dependencies:

  ```
  bundle install
  ```

3. Run tests:

  ```
  rake
  ```
